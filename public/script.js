// script.js
const images = document.querySelectorAll('.image');

images.forEach(image => {
    image.addEventListener('click', () => {
        // Ganti ini dengan kode untuk tampilan gambar lebih besar
        alert('Tampilkan gambar lebih besar di sini');
    });
});
